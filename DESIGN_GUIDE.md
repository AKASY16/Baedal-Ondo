# 배달온도 디자인 가이드

## 제품 문장

배달 매장을 운영하는 사장님이 영업 전이나 주문이 뜸할 때, 현재 날씨·시간·요일·대기질을 바탕으로 오늘의 주문 기대치와 대응 방향을 10초 안에 확인하는 운영 판단 도구다.

## 참고 제품과 차용 범위

### 네이버 스마트플레이스

- 현재 관리 중인 가게가 무엇인지 항상 분명하게 표시한다.
- 가게 선택과 가게 정보 수정처럼 운영에 필요한 동선을 화면 상단에 둔다.
- 장식보다 업무 정보의 이름과 위계를 우선한다.

### 기상청 날씨누리 상황판

- 현재 상태를 화면에서 가장 먼저 읽히는 정보로 둔다.
- 색상은 꾸밈이 아니라 상태의 의미를 구별하는 용도로 사용한다.
- 결과 아래에 판단 근거를 같은 형식의 행으로 제공한다.

### Square Dashboard

- 가장 중요한 수치를 크게 보여주고 바로 옆에 사용자가 취할 행동을 제시한다.
- 보조 정보는 정렬된 행과 구분선을 사용해 빠르게 비교할 수 있게 한다.
- 작은 사업자가 반복해서 사용하는 운영 도구답게 정보 밀도를 유지한다.

특정 제품의 시각 표현이나 레이아웃을 그대로 복제하지 않는다. 각 제품이 해결한 정보 설계 방식만 참고한다.

## 배달온도의 시각 원칙

1. 화면은 `현재 주문 기대치 → 운영 제안 → 판단 근거` 순서로 읽힌다.
2. 기본 배경은 따뜻한 무채색을 사용하고 체온계의 코럴 색상을 핵심 상태에만 쓴다.
3. 상태 색상은 배달온도 점수, 상태 표시, 관련 문구에만 제한한다.
4. 카드를 쌓기보다 구분선과 정렬로 정보 덩어리를 나눈다.
5. 모서리는 0~4px 범위로 유지한다.
6. 화면 제목보다 실제 가게명과 현재 점수가 더 먼저 읽혀야 한다.
7. 데이터 요인은 동일한 열 구조를 사용해 한눈에 비교할 수 있게 한다.
8. 모바일에서는 점수, 운영 제안, 판단 근거 순서를 그대로 유지한다.

## 사용하지 않는 표현

- UI 영역의 그라데이션과 글로우
- 유리 효과와 반투명 플로팅 카드
- 장식용 그림자
- 의미 없이 큰 둥근 모서리와 알약 모양 요소
- 카드 안에 다시 들어가는 카드
- 장식용 이모지와 서로 다른 스타일의 아이콘 혼용
- 보라색·파란색 중심의 범용 SaaS 팔레트
- 기능과 연결되지 않는 차트와 통계
- 같은 내용을 제목, 배지, 설명문으로 반복하는 구성

## 기본 토큰

- Canvas: `#f1efe9`
- Surface: `#faf9f5`
- Ink: `#1d1d1b`
- Muted: `#6e6c65`
- Line: `#cbc8be`
- Brand/High: `#e34d3e`
- Medium: `#a86b00`
- Low: `#356ca8`
- Positive factor: `#176c4b`
- Negative factor: `#2f67a8`
- Body font: SUIT

## 화면별 밀도

- 대시보드: 한 화면에서 판단이 끝나도록 높은 정보 밀도를 유지한다.
- 등록·수정: 업무용 서식처럼 필드와 저장 동선을 명료하게 구성한다.
- 로그인: 브랜드 문장을 보여주되 장식은 최소화한다.
- 오류·빈 상태: 문제와 다음 행동만 짧게 제시한다.

새 화면을 만들 때 이 가이드의 형태를 그대로 복사하지 말고, 제품 문장과 정보 순서를 먼저 적용한 뒤 필요한 구성만 선택한다.
